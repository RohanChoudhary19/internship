const obj = {};

// Add two keys
obj.name = "John";
obj.age = 25;

console.log(obj);
const obj = {};

// add two keys
obj.name = "John";
obj.age = 25;

console.log(obj);
user.subject ="maths";
user.grade ="a++";
console.log(user);
consolo.log(user.dog());
// higher-order function taking a function as an argument
function executeAction(ation){
    console.log("Executing action...");
    action();// calls the function passed to it
}
 executeAction(hellobro);
 function hellobro(){
    console.log("hello bro!");
 }


