function createBankAccount(initialBalance){
    let balance = intialBalace;

    return{
        deposit(amount){
            balance += valur;
            console.log('Deposited:${value},currnet Balance:${balance}');
        },
        withdeaw(value){
            balance -= value;
            console.log('withdraw:${value},currnet balance:${balance}');
        },
        checkbalance(){
            console.log('current balance:${balance}');

        },
        };



        }
        let user = createbankAccount(100);
        user.deposite(50);
        user.withdraw(100);
        user.checkbalance();

        console.log(user.balance); // undefined
    
